file:
    upload-dir: uploads
logging:
    file:
        name: /logs/spring-board-app.log
    level:
        root: info
mybatis:
    configuration:
        map-underscore-to-camel-case: true
    mapper-locations: classpath:mappers/**/*.xml
server:
    servlet:
        context-path: /
spring:
    application:
        name: spring-board-app
        version: ${version:0.0.6}
    datasource:
        driver-class-name: com.mysql.cj.jdbc.Driver
        password: 1q2w3E4R!
        url: jdbc:mysql://3.35.24.141:3306/kmarket?serverTimezone=Asia/Seoul&characterEncoding=UTF-8&useSSL=false&allowPublicKeyRetrieval=true
        username: kmarket_user
    jpa:
        database: mysql
        hibernate:
            ddl-auto: none
            naming:
                physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
        properties:
            hibernate:
                format_sql: true
                show_sql: true
    mail:
        host: smtp.gmail.com
        password: ncnebtboedkqgncn
        port: 587
        properties:
            mail:
                smtp:
                    auth: true
                    ssl:
                        trust: smtp.gmail.com
                    starttls:
                        enable: true
                        required: true
                    timeout: 3000
        username: heocoding@gmail.com
    servlet:
        multipart:
            enabled: true
            location: src/main/resources/static/uploads
            max-file-size: 10MB
            max-request-size: 50MB
    thymeleaf:
        cache: false
        prefix: classpath:/templates/
