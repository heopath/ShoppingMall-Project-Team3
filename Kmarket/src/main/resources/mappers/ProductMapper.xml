<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.co.springkmarketapp.dao.product.ProductDAO">

    <!-- 메인 상품 공통 조회 -->
    <sql id="mainProductColumns">
        SELECT
        p.product_no     AS productNo,
        p.product_name   AS productName,
        p.basic_desc     AS basicDesc,
        p.brand          AS brand,
        p.price          AS price,
        p.discount_rate  AS discountRate,
        p.point          AS point,
        p.delivery_fee   AS deliveryFee,
        p.view_count     AS viewCount,
        p.sold_count     AS soldCount,

        <!-- 베스트상품 2~5위용 -->
        MAX(
        CASE
        WHEN pi.image_type = 'THUMB1'
        THEN pi.image_path
        END
        ) AS thumb1Path,

        <!-- 메인 카드 + 베스트상품 1위용 -->
        COALESCE(
        MAX(
        CASE
        WHEN pi.image_type = 'THUMB2'
        THEN pi.image_path
        END
        ),
        MAX(
        CASE
        WHEN pi.image_type = 'THUMB1'
        THEN pi.image_path
        END
        ),
        MAX(
        CASE
        WHEN pi.image_type = 'MAIN'
        THEN pi.image_path
        END
        )
        ) AS imagePath,

        ROUND(IFNULL(AVG(pr.rating), 0), 1) AS avgRating,
        COUNT(DISTINCT pr.review_no) AS reviewCount

        FROM product p

        LEFT JOIN product_image pi
        ON p.product_no = pi.product_no

        LEFT JOIN product_review pr
        ON p.product_no = pr.product_no

        WHERE p.status = '판매중'

        GROUP BY
        p.product_no,
        p.product_name,
        p.basic_desc,
        p.brand,
        p.price,
        p.discount_rate,
        p.point,
        p.delivery_fee,
        p.view_count,
        p.sold_count
    </sql>


    <!-- 베스트상품 5개 : 판매 건수 순 -->
    <select id="selectBestProducts"
            resultType="kr.co.springkmarketapp.dto.product.ProductMainDTO">

        <include refid="mainProductColumns"/>

        ORDER BY p.sold_count DESC
        LIMIT 5

    </select>


    <!-- 히트상품 8개 : 조회수 순 -->
    <select id="selectHitProducts"
            resultType="kr.co.springkmarketapp.dto.product.ProductMainDTO">

        <include refid="mainProductColumns"/>

        ORDER BY p.view_count DESC
        LIMIT 8

    </select>


    <!-- 추천상품 8개 : 평균 평점 순 -->
    <select id="selectRecommendProducts"
            resultType="kr.co.springkmarketapp.dto.product.ProductMainDTO">

        <include refid="mainProductColumns"/>

        ORDER BY avgRating DESC, reviewCount DESC
        LIMIT 8

    </select>


    <!-- 최신상품 8개 : 등록일 최신 순 -->
    <select id="selectNewProducts"
            resultType="kr.co.springkmarketapp.dto.product.ProductMainDTO">

        <include refid="mainProductColumns"/>

        ORDER BY p.reg_date DESC
        LIMIT 8

    </select>


    <!-- 인기상품 8개 : 판매량 + 조회수 종합 순 -->
    <select id="selectPopularProducts"
            resultType="kr.co.springkmarketapp.dto.product.ProductMainDTO">

        <include refid="mainProductColumns"/>

        ORDER BY (p.sold_count * 3 + p.view_count) DESC
        LIMIT 8

    </select>


    <!-- 할인상품 8개 : 할인율 높은 순 -->
    <select id="selectDiscountProducts"
            resultType="kr.co.springkmarketapp.dto.product.ProductMainDTO">

        <include refid="mainProductColumns"/>

        ORDER BY p.discount_rate DESC, p.price DESC
        LIMIT 8

    </select>

    <!-- 2차 카테고리 상품 목록 조회 -->
    <select id="selectProductsByCategory"
            resultType="kr.co.springkmarketapp.dto.product.ProductListDTO">

        SELECT
        p.product_no AS productNo,
        p.product_name AS productName,
        p.basic_desc AS basicDesc,

        p.price AS price,
        p.discount_rate AS discountRate,
        p.delivery_fee AS deliveryFee,

        img.image_path AS imagePath,

        sp.company_name AS sellerName,

        /* 현재 상품 리뷰 통계 */
        IFNULL(rv.avg_rating, 0) AS avgRating,
        IFNULL(rv.review_count, 0) AS reviewCount,

        /* 판매자 전체 상품 기준 통계 */
        IFNULL(ss.seller_avg_rating, 0) AS sellerAvgRating,
        IFNULL(ss.seller_review_count, 0) AS sellerReviewCount,
        IFNULL(ss.seller_sold_count, 0) AS sellerSoldCount,

        /* 판매자 뱃지 판정 */
        CASE
        WHEN ss.seller_avg_rating >= 4.5
        AND ss.seller_review_count >= 10
        THEN 'SATISFACTION'

        WHEN ss.seller_sold_count >= 2000
        THEN 'BEST'

        WHEN ss.seller_avg_rating >= 4.0
        OR ss.seller_sold_count >= 1600
        THEN 'EXCELLENT'

        ELSE 'GENERAL'
        END AS sellerBadgeCode

        FROM product p

        /* 상품 판매자 정보 */
        JOIN seller_profile sp
        ON p.seller_no = sp.seller_no

        /* 상품 대표 썸네일 1장 */
        LEFT JOIN (
        SELECT
        product_no,
        MAX(
        CASE
        WHEN image_type = 'THUMB1'
        THEN image_path
        END
        ) AS image_path
        FROM product_image
        GROUP BY product_no
        ) img
        ON p.product_no = img.product_no

        /* 현재 상품의 리뷰 평균 / 리뷰 수 */
        LEFT JOIN (
        SELECT
        product_no,
        ROUND(AVG(rating), 1) AS avg_rating,
        COUNT(review_no) AS review_count
        FROM product_review
        GROUP BY product_no
        ) rv
        ON p.product_no = rv.product_no

        /*
        판매자 전체 통계
        판매량과 리뷰 통계를 분리해서 계산한다.
        그래야 리뷰 개수만큼 sold_count가 중복 합산되지 않는다.
        */
        LEFT JOIN (
        SELECT
        sales.seller_no,
        sales.seller_sold_count,
        IFNULL(review.seller_avg_rating, 0) AS seller_avg_rating,
        IFNULL(review.seller_review_count, 0) AS seller_review_count

        FROM (
        /* 판매자별 누적 판매량 */
        SELECT
        seller_no,
        SUM(sold_count) AS seller_sold_count
        FROM product
        GROUP BY seller_no
        ) sales

        LEFT JOIN (
        /* 판매자별 전체 리뷰 평균 / 리뷰 수 */
        SELECT
        p.seller_no,
        ROUND(AVG(pr.rating), 1) AS seller_avg_rating,
        COUNT(pr.review_no) AS seller_review_count
        FROM product p
        JOIN product_review pr
        ON p.product_no = pr.product_no
        GROUP BY p.seller_no
        ) review
        ON sales.seller_no = review.seller_no

        ) ss
        ON p.seller_no = ss.seller_no

        WHERE p.cate_no = #{cateNo}
        AND p.status = '판매중'

        <choose>
            <when test="sort == 'sold'">
                ORDER BY p.sold_count DESC, p.product_no DESC
            </when>

            <when test="sort == 'lowPrice'">
                ORDER BY (p.price - FLOOR(p.price * p.discount_rate / 100)) ASC
            </when>

            <when test="sort == 'highPrice'">
                ORDER BY (p.price - FLOOR(p.price * p.discount_rate / 100)) DESC
            </when>

            <when test="sort == 'highRating'">
                ORDER BY rv.avg_rating DESC, rv.review_count DESC
            </when>

            <when test="sort == 'lowRating'">
                ORDER BY
                CASE WHEN rv.avg_rating IS NULL THEN 1 ELSE 0 END,
                rv.avg_rating ASC
            </when>

            <when test="sort == 'recent'">
                ORDER BY p.reg_date DESC
            </when>

            <otherwise>
                ORDER BY p.sold_count DESC, p.product_no DESC
            </otherwise>
        </choose>

        LIMIT #{size} OFFSET #{offset}

    </select>


    <!-- 해당 카테고리 상품 수 -->
    <select id="selectCountByCategory" resultType="int">

        SELECT COUNT(*)
        FROM product
        WHERE cate_no = #{cateNo}
        AND status = '판매중'

    </select>

    <!-- 상품 상세 페이지 정보 -->
    <select id="selectProductView"
            resultType="kr.co.springkmarketapp.dto.product.ProductViewDTO">

        SELECT
        p.product_no AS productNo,
        p.seller_no AS sellerNo,
        p.cate_no AS cateNo,

        p.product_name AS productName,
        p.basic_desc AS basicDesc,
        p.brand AS brand,

        p.price AS price,
        p.discount_rate AS discountRate,
        p.point AS point,
        p.stock AS stock,
        p.delivery_fee AS deliveryFee,
        p.status AS status,

        pn.origin AS origin,

        img.image_path AS imagePath,

        sp.company_name AS sellerName,

        IFNULL(rv.avg_rating, 0) AS avgRating,
        IFNULL(rv.review_count, 0) AS reviewCount

        FROM product p

        JOIN seller_profile sp
        ON p.seller_no = sp.seller_no

        LEFT JOIN product_notice pn
        ON p.product_no = pn.product_no

        LEFT JOIN (
        SELECT
        product_no,
        MAX(
        CASE
        WHEN image_type = 'THUMB3'
        THEN image_path
        END
        ) AS image_path
        FROM product_image
        GROUP BY product_no
        ) img
        ON p.product_no = img.product_no

        LEFT JOIN (
        SELECT
        product_no,
        ROUND(AVG(rating), 1) AS avg_rating,
        COUNT(review_no) AS review_count
        FROM product_review
        GROUP BY product_no
        ) rv
        ON p.product_no = rv.product_no

        WHERE p.product_no = #{productNo}
        AND p.status = '판매중'

    </select>

    <!-- 상품 옵션 -->
    <select id="selectProductOptions"
            resultType="kr.co.springkmarketapp.dto.product.ProductOptionDTO">

        SELECT
        option_no       AS optionNo,
        product_no      AS productNo,
        option_group_no AS optionGroupNo,
        option_name     AS optionName,
        option_value    AS optionValue,
        sort_order      AS sortOrder
        FROM product_option
        WHERE product_no = #{productNo}
        ORDER BY option_group_no ASC, sort_order ASC, option_no ASC

    </select>

    <!-- 상품 상세 이미지 -->
    <select id="selectDetailImages"
            resultType="kr.co.springkmarketapp.dto.product.ProductImageDTO">

        SELECT
        image_no AS imageNo,
        product_no AS productNo,
        image_type AS imageType,
        image_path AS imagePath,
        sort_order AS sortOrder
        FROM product_image
        WHERE product_no = #{productNo}
        AND image_type = 'DETAIL'
        ORDER BY sort_order ASC, image_no ASC

    </select>

    <!-- 상품정보 상세고시 -->
    <select id="selectProductNotice"
            resultType="kr.co.springkmarketapp.dto.product.ProductNoticeDTO">

        SELECT
        notice_no AS noticeNo,
        product_no AS productNo,
        product_status AS productStatus,
        tax_type AS taxType,
        receipt_type AS receiptType,
        business_type AS businessType,
        origin AS origin
        FROM product_notice
        WHERE product_no = #{productNo}

    </select>

    <!-- 상품 리뷰 목록 -->
    <select id="selectProductReviewList"
            resultType="kr.co.springkmarketapp.dto.product.ProductReviewDTO">

        SELECT
        pr.review_no AS reviewNo,
        pr.order_item_no AS orderItemNo,
        pr.member_no AS memberNo,
        pr.product_no AS productNo,
        pr.rating AS rating,
        pr.content AS content,
        pr.reg_date AS regDate,

        m.member_id AS memberId,

        ri.image_path AS imagePath

        FROM product_review pr

        JOIN member m
        ON pr.member_no = m.member_no

        LEFT JOIN review_image ri
        ON ri.review_image_no = (
        SELECT ri2.review_image_no
        FROM review_image ri2
        WHERE ri2.review_no = pr.review_no
        ORDER BY ri2.sort_order ASC, ri2.review_image_no ASC
        LIMIT 1
        )

        WHERE pr.product_no = #{productNo}

        ORDER BY pr.reg_date DESC, pr.review_no DESC

        LIMIT #{pageRequestDTO.offset}, #{pageRequestDTO.size}

    </select>

    <!-- 상품 리뷰 총 개수 -->
    <select id="selectProductReviewCount"
            resultType="int">

        SELECT COUNT(*)
        FROM product_review
        WHERE product_no = #{productNo}

    </select>

</mapper>